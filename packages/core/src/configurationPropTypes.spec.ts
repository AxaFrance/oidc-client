import { configurationPropTypes } from './configurationPropTypes';

describe('configurationPropTypes tests suite', () => {
  it('configurationPropTypes should not be null', () => {
    expect(configurationPropTypes).not.toBeNull();
  });
});
